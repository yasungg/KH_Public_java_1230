package TV만들기;

public class TvClass {
    /*private boolean isON; // TV의 전원에 대한 값 저장, 접근제한자 설정
    private int channel;
    private int volume;
    TvClass(){
    }
    TvClass(boolean isON, int cnl, int vol) {
        this.isON = isON;
        channel = cnl;
        volume = vol;
    }
    void SetON(boolean isON) { // 외부에서 전원값을 변경할 수 있도록 제공되는 메소드
        this.isON = isON; //매개변수로 전달받은 값을 인스턴스 필드에 저장
        String onOFFStr = (isON) ? "ON" : "OFF";
        System.out.println("전원 : " + onOFFStr);ㅔ
    }
    void SetChannel(int cnl) {
        if(cnl >= 0 && cnl <= 999) { //해당 필드에 허용되지 않는 값이 들어오지 않도록 조건을 정함
            channel = cnl;
            System.out.println("채널 " + cnl);
        } else {
            System.out.println("잘못된 채널을 입력하였습니다.");
        }
    }
    void SetVolume(int vol) {
        if(vol >= 1 && vol <= 100) {
            volume = vol;
            System.out.println("볼륨 : " + vol);
        } else if(vol == 0) System.out.println("음소거");
        else System.out.println("음량을 잘못 입력하였습니다.");
    }
    void getTV() {
        String onOFFStr = (isON) ? "ON" : "OFF";
        System.out.println("===TV 정보===");
        System.out.println("전원 : " + isON);
        System.out.println("채널 : " + channel);
        System.out.println("볼륨 : " + volume);*/
    }
